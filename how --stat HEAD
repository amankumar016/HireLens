[1mdiff --git a/server.ts b/server.ts[m
[1mindex b77b45a..28bb517 100644[m
[1m--- a/server.ts[m
[1m+++ b/server.ts[m
[36m@@ -3,6 +3,7 @@[m [mimport path from "path";[m
 import dotenv from "dotenv";[m
 import { GoogleGenAI, Type } from "@google/genai";[m
 import { createServer as createViteServer } from "vite";[m
[32m+[m[32mimport { runLocalIngestionFallback, runLocalRankingHeuristics } from "./src/backend/utils/fallbacks";[m
 import { INITIAL_CANDIDATES } from "./src/data/candidates";[m
 [m
 // Load environment variables[m
[36m@@ -21,6 +22,35 @@[m [mconst ai = new GoogleGenAI({[m
   },[m
 });[m
 [m
[32m+[m[32m// Helper: robust text-to-JSON generation with auto-retry and exponential backoff[m
[32m+[m[32masync function generateContentWithRetry(options: any, maxRetries = 3, initialDelay = 500): Promise<any> {[m
[32m+[m[32m  let attempt = 0;[m
[32m+[m[32m  while (true) {[m
[32m+[m[32m    try {[m
[32m+[m[32m      return await ai.models.generateContent(options);[m
[32m+[m[32m    } catch (error: any) {[m
[32m+[m[32m      attempt++;[m
[32m+[m[32m      const errorMessage = error?.message || (typeof error === "object" ? JSON.stringify(error) : String(error));[m
[32m+[m[32m      const isRetryable =[m[41m [m
[32m+[m[32m        errorMessage.includes("503") ||[m[41m [m
[32m+[m[32m        errorMessage.includes("500") ||[m[41m [m
[32m+[m[32m        errorMessage.includes("UNAVAILABLE") ||[m[41m [m
[32m+[m[32m        errorMessage.includes("429") ||[m[41m [m
[32m+[m[32m        errorMessage.includes("RESOURCE_EXHAUSTED") ||[m[41m [m
[32m+[m[32m        errorMessage.includes("demand") ||[m
[32m+[m[32m        errorMessage.includes("temporary");[m
[32m+[m
[32m+[m[32m      if (isRetryable && attempt < maxRetries) {[m
[32m+[m[32m        const delay = initialDelay * Math.pow(2, attempt - 1) * (0.8 + Math.random() * 0.4); // exponential backoff with jitter[m
[32m+[m[32m        console.warn(`[Gemini API] Attempt ${attempt} failed with retryable error: ${errorMessage.substring(0, 150)}. Retrying in ${Math.round(delay)}ms...`);[m
[32m+[m[32m        await new Promise(resolve => setTimeout(resolve, delay));[m
[32m+[m[32m        continue;[m
[32m+[m[32m      }[m
[32m+[m[32m      throw error;[m
[32m+[m[32m    }[m
[32m+[m[32m  }[m
[32m+[m[32m}[m
[32m+[m
 // Use standard middleware[m
 app.use(express.json());[m
 [m
[36m@@ -67,8 +97,10 @@[m [minterface Candidate {[m
 // Seed Initial Candidates[m
 let candidates: Candidate[] = JSON.parse(JSON.stringify(INITIAL_CANDIDATES));[m
 [m
[31m-// Helper Function: Local Resume Ingestion Parser (Failsafe Fallback)[m
[31m-function runLocalIngestionFallback(resumeText: string): Candidate {[m
[32m+[m
[32m+[m[32m// Backend utility functions moved to src/backend/utils/fallbacks.ts[m
[32m+[m[32mimport { runLocalIngestionFallback, runLocalRankingHeuristics } from "./src/backend/utils/fallbacks";[m
[32m+[m
   const adjectives = ["Quantum", "Kernel", "Async", "Serverless", "Vector", "Distributed", "Reactive", "Linear", "Consensus", "Idempotent", "Zero-Copy", "Telemetry", "Polymorphic", "Event", "Pipeline", "Cluster", "Memory", "Signal"];[m
   const nouns = ["Architect", "Crafter", "Weaver", "Maestro", "Refinery", "Forge", "Engine", "Sync", "Mesh", "Node", "Core", "Optimizer", "Shield", "Gateway", "Vault", "Beacon", "Pioneer", "Sentry", "Vanguard"];[m
   [m
[36m@@ -354,7 +386,7 @@[m [mapp.post("/api/ingest", async (req, res) => {[m
     ${resumeText}`;[m
 [m
     // Agent 1 uses 'gemini-3.5-flash'[m
[31m-    const response = await ai.models.generateContent({[m
[32m+[m[32m    const response = await generateContentWithRetry({[m
       model: "gemini-3.5-flash",[m
       contents: prompt,[m
       config: {[m
[36m@@ -463,8 +495,13 @@[m [mapp.post("/api/rank", async (req, res) => {[m
     let isFallbackActive = false;[m
 [m
     try {[m
[31m-      const evaluationPrompt = `You are Agent 2: Evaluation Matrix. Your job is deep multi-axis reasoning, conceptual alignment scoring (0-100), and contextual logic verification.[m
[31m-      Evaluate how well each of the listed candidates matches the Job Specification across three distinct dimensions.[m
[32m+[m[32m      const evaluationPrompt = `You are the Coordinator of a Collaborative Consensus Panel of Virtual Specialized Sub-Agents. Your job is deep multi-axis reasoning, cross-verification, conceptual alignment scoring (0-100), and dynamic ranking based on three distinct expert personas:[m
[32m+[m
[32m+[m[32m      1. Virtual Agent A: Chief Systems Architect (Focuses strictly on Tech Stack Compatibility, software complexity, architectural maturity, and the "Ghost Competencies" parsed during ingestion).[m
[32m+[m[32m      2. Virtual Agent B: Talent & Behavioral Trajectory Analyst (Focuses strictly on execution velocity, career trajectory, ownership scale, leadership maturity, and project progression).[m
[32m+[m[32m      3. Virtual Agent C: Domain Subject Matter Expert (Focuses strictly on industry alignment, business-domain fit, regulatory/technical domain nuance, and practical product-market experience).[m
[32m+[m
[32m+[m[32m      Evaluate how well each of the listed candidates matches the Job Specification across these three specialized dimensions. Let the sub-agents debate and reach a consensus score.[m
 [m
       JOB SPECIFICATION:[m
       ${jobDescription}[m
[36m@@ -480,10 +517,10 @@[m [mapp.post("/api/rank", async (req, res) => {[m
       `).join("\n\n")}[m
 [m
       OUTPUT REQUISITES:[m
[31m-      Evaluate and return scores from 0 to 100 for each candidate.[m
[31m-      Provide precise structural justifications for your ratings. Output strict JSON format with an array of evaluations, one for each candidate ID listed above.`;[m
[32m+[m[32m      Evaluate and return consensus scores from 0 to 100 for each candidate across the three agent dimensions.[m
[32m+[m[32m      Provide precise structural justifications drafted by the respective virtual agent for their rating. Output strict JSON format with an array of evaluations, one for each candidate ID listed above.`;[m
 [m
[31m-      const response = await ai.models.generateContent({[m
[32m+[m[32m      const response = await generateContentWithRetry({[m
         model: "gemini-3.5-flash", // Fast & fully accurate for this task, avoiding paid model flow requirements[m
         contents: evaluationPrompt,[m
         config: {[m
[1mdiff --git a/src/App.tsx b/src/App.tsx[m
[1mindex a521e27..471d92a 100644[m
[1m--- a/src/App.tsx[m
[1m+++ b/src/App.tsx[m
[36m@@ -41,6 +41,7 @@[m [mimport AnimatedScore from "./components/AnimatedScore";[m
 import KPIDashboardWidgets from "./components/KPIDashboardWidgets";[m
 import ComparisonVisualizer from "./components/ComparisonVisualizer";[m
 import { TiltCard, MagneticWrapper, TextCharReveal, CursorSpotlight, Preloader, CustomCursor, ScrollEngine3D, DynamicMetricsAccordion, InteractiveNavLink } from "./components/KineticInteractions";[m
[32m+[m[32mimport ThemeToggle from "./components/ThemeToggle";[m
 [m
 export default function App() {[m
   // Application State[m
[36m@@ -88,8 +89,6 @@[m [mexport default function App() {[m
   const [bulkProcessing, setBulkProcessing] = useState(false);[m
   const [bulkProgress, setBulkProgress] = useState(0);[m
   const [bulkStep, setBulkStep] = useState("");[m
[31m-  const [isFullScreen, setIsFullScreen] = useState(false);[m
[31m-  const [showIngestionSidebar, setShowIngestionSidebar] = useState(false);[m
 [m
   const toggleBulkSelect = (id: string) => {[m
     setBulkSelectedIds((prev) =>[m
[36m@@ -222,27 +221,6 @@[m [mexport default function App() {[m
     fetchCandidates();[m
   }, []);[m
 [m
[31m-  // Exit fullscreen on Escape key and lock body scroll[m
[31m-  useEffect(() => {[m
[31m-    const handleKeyDown = (e: KeyboardEvent) => {[m
[31m-      if (e.key === "Escape") {[m
[31m-        setIsFullScreen(false);[m
[31m-      }[m
[31m-    };[m
[31m-    window.addEventListener("keydown", handleKeyDown);[m
[31m-    [m
[31m-    if (isFullScreen) {[m
[31m-      document.body.style.overflow = "hidden";[m
[31m-    } else {[m
[31m-      document.body.style.overflow = "";[m
[31m-    }[m
[31m-[m
[31m-    return () => {[m
[31m-      window.removeEventListener("keydown", handleKeyDown);[m
[31m-      document.body.style.overflow = "";[m
[31m-    };[m
[31m-  }, [isFullScreen]);[m
[31m-[m
   const fetchCandidates = async () => {[m
     setLoadingCandidates(true);[m
     try {[m
[36m@@ -1450,26 +1428,17 @@[m [mexport default function App() {[m
           {/* BENTO CARD 4: Live Leaderboard standings */}[m
           <div [m
             id="bento-leaderboard" [m
[31m-            className={`transition-all duration-300 relative ${[m
[31m-              isFullScreen[m
[31m-                ? "fixed inset-0 z-50 bg-[#FDFCF8] p-6 md:p-8 flex flex-col md:flex-row gap-6 overflow-hidden"[m
[31m-                : "p-6 rounded-3xl bg-white border border-[#E5E2D9] shadow-sm flex flex-col justify-between"[m
[31m-            }`}[m
[32m+[m[32m            className="p-6 rounded-3xl bg-white border border-[#E5E2D9] shadow-sm flex flex-col justify-between relative"[m
           >[m
[31m-            <div className={isFullScreen ? (showIngestionSidebar ? "flex-grow flex flex-col gap-4 overflow-hidden h-full w-full md:w-[65%] lg:w-[70%]" : "flex-grow flex flex-col gap-4 overflow-hidden h-full w-full") : "w-full flex flex-col h-full justify-between"}>[m
[32m+[m[32m            <div className="w-full flex flex-col h-full justify-between">[m
               <div className="flex flex-col sm:flex-row justify-between sm:items-center gap-4 mb-4">[m
                 <div>[m
                   <div className="flex items-center gap-2">[m
                     <div className="text-[10px] font-mono uppercase tracking-wider text-stone-400">[m
                       Evaluation Scoreboard[m
                     </div>[m
[31m-                    {isFullScreen && ([m
[31m-                      <span className="bg-[#3D52A0]/10 text-[#3D52A0] text-[9px] px-2 py-0.5 rounded-full font-mono font-bold uppercase tracking-wide animate-pulse">[m
[31m-                        IMMEDIATE DIAGNOSTIC VIEW ACTIVE[m
[31m-                      </span>[m
[31m-                    )}[m
                   </div>[m
[31m-                  <h2 className={`font-display font-bold text-[#2D2926] ${isFullScreen ? "text-2xl" : "text-base"}`}>[m
[32m+[m[32m                  <h2 className="font-display font-bold text-[#2D2926] text-base">[m
                     Talent Leaderboard Alignment Standings[m
                   </h2>[m
                 </div>[m
[36m@@ -1520,41 +1489,6 @@[m [mexport default function App() {[m
                     <Download className="w-3.5 h-3.5" />[m
                     <span>Export CSV</span>[m
                   </button>[m
[31m-[m
[31m-                  {isFullScreen && ([m
[31m-                    <button[m
[31m-                      id="toggle-ingestion-sidebar-btn"[m
[31m-                      onClick={() => setShowIngestionSidebar(!showIngestionSidebar)}[m
[31m-                      className={`flex items-center gap-1.5 px-3 py-1.5 rounded-xl text-xs font-semibold border active:scale-95 transition-all shadow-xs cursor-pointer ${[m
[31m-                        showIngestionSidebar[m
[31m-                          ? "bg-teal-700 text-white border-teal-600 hover:bg-teal-800"[m
[31m-                          : "text-teal-800 bg-teal-50 hover:bg-teal-100 border-teal-200"[m
[31m-                      }`}[m
[31m-                      title="Toggle Ingestion Lab Sidebar"[m
[31m-                    >[m
[31m-                      <Layers className="w-3.5 h-3.5" />[m
[31m-                      <span>{showIngestionSidebar ? "Hide Ingestion Lab" : "Open Ingestion Lab"}</span>[m
[31m-                    </button>[m
[31m-                  )}[m
[31m-[m
[31m-                  <button[m
[31m-                    id="fullscreen-mode-btn"[m
[31m-                    onClick={() => setIsFullScreen(!isFullScreen)}[m
[31m-                    className={`flex items-center gap-1.5 px-3 py-1.5 rounded-xl text-xs font-semibold border active:scale-95 transition-all shadow-xs cursor-pointer ${[m
[31m-                      isFullScreen[m
[31m-                        ? "bg-rose-600 text-white border-rose-500 hover:bg-rose-700"[m
[31m-                        : "text-stone-600 bg-stone-50 hover:bg-stone-100 border-stone-200"[m
[31m-                    }`}[m
[31m-                    title="Toggle Full Screen Mode"[m
[31m-                  >[m
[31m-                    {isFullScreen ? <Minimize2 className="w-3.5 h-3.5" /> : <Maximize2 className="w-3.5 h-3.5" />}[m
[31m-                    <span>{isFullScreen ? "Exit Full Screen" : "Full Screen"}</span>[m
[31m-                  </button>[m
[31m-                  {isFullScreen && ([m
[31m-                    <span className="text-[10px] text-stone-400 font-mono hidden md:inline bg-stone-100 px-2 py-1 rounded border border-stone-200">[m
[31m-                      Press <kbd className="font-bold">Esc</kbd>[m
[31m-                    </span>[m
[31m-                  )}[m
                 </div>[m
               </div>[m
 [m
[36m@@ -1686,7 +1620,7 @@[m [mexport default function App() {[m
             ) : ([m
               <>[m
                 {/* Mobile Breakpoint Card-Based View */}[m
[31m-                <div className={`block md:hidden space-y-3 ${isFullScreen ? "overflow-y-auto max-h-[60vh] pb-4" : ""}`}>[m
[32m+[m[32m                <div className="block md:hidden space-y-3">[m
                   {filteredCandidates.length === 0 ? ([m
                     <div className="py-12 text-center text-xs text-stone-400 font-sans italic bg-[#FDFCF8]/50 border border-stone-100 rounded-2xl">[m
                       No candidates match current filter.[m
[36m@@ -1884,11 +1818,9 @@[m [mexport default function App() {[m
                 </div>[m
 [m
                 {/* Desktop & Tablet Table with Horizontal Scrolling wrapper */}[m
[31m-                <div className={`hidden md:block overflow-x-hidden overflow-y-auto rounded-2xl border border-stone-200/90 bg-[#FDFCF8]/30 p-2 shadow-sm transition-all duration-300 ${[m
[31m-                  isFullScreen ? "flex-1 min-h-0" : "max-h-[62vh]"[m
[31m-                }`}>[m
[32m+[m[32m                <div className="hidden md:block overflow-x-hidden overflow-y-auto rounded-2xl border border-stone-200/90 bg-[#FDFCF8]/30 p-2 shadow-sm max-h-[62vh]">[m
                   <table className="w-full text-left border-collapse table-auto bg-white rounded-xl overflow-hidden border border-stone-150/60">[m
[31m-                    <thead className={isFullScreen ? "sticky top-0 z-10 bg-stone-50 shadow-xs" : "bg-stone-50/80"}>[m
[32m+[m[32m                    <thead className="bg-stone-50/80">[m
                       <tr className="text-stone-500 text-[10px] font-mono uppercase tracking-wider border-b border-[#E5E2D9]">[m
                         {bulkProcessMode && ([m
                           <th className="py-3 px-4 text-center w-14 select-none">Select</th>[m
[36m@@ -2233,31 +2165,6 @@[m [mexport default function App() {[m
               </>[m
             )}[m
             </div>[m
[31m-[m
[31m-            {/* Right Portion of Fullscreen: Collapsible Ingestion Lab */}[m
[31m-            {isFullScreen && showIngestionSidebar && ([m
[31m-              <div className="w-full md:w-[35%] lg:w-[350px] flex-shrink-0 flex flex-col h-full overflow-hidden bg-white border border-[#E5E2D9] rounded-3xl p-5 shadow-lg animate-fade-in">[m
[31m-                <div className="mb-4 flex items-center justify-between">[m
[31m-                  <div>[m
[31m-                    <div className="text-[10px] font-mono uppercase tracking-wider text-[#024950] font-bold">[m
[31m-                      Ingested Resumes Feed[m
[31m-                    </div>[m
[31m-                    <h3 className="text-sm font-display font-bold text-[#003135]">[m
[31m-                      The Ingestion Lab (Stage 1)[m
[31m-                    </h3>[m
[31m-                  </div>[m
[31m-                  <button[m
[31m-                    onClick={() => setShowIngestionSidebar(false)}[m
[31m-                    className="p-1 hover:bg-stone-100 rounded-lg text-stone-400 hover:text-stone-700 transition-colors cursor-pointer"[m
[31m-                  >[m
[31m-                    <X className="w-4 h-4" />[m
[31m-                  </button>[m
[31m-                </div>[m
[31m-                <div className="flex-grow overflow-y-auto pr-1">[m
[31m-                  <CandidateIngestionForm onIngestSuccess={handleNewCandidateIngested} />[m
[31m-                </div>[m
[31m-              </div>[m
[31m-            )}[m
           </div>[m
 [m
           {/* BENTO CARD 5: Selected Candidate Deep-Dive Verification Matrix */}[m
[1mdiff --git a/src/components/CandidateIngestionForm.tsx b/src/components/CandidateIngestionForm.tsx[m
[1mindex 8402beb..a18c16c 100644[m
[1m--- a/src/components/CandidateIngestionForm.tsx[m
[1m+++ b/src/components/CandidateIngestionForm.tsx[m
[36m@@ -22,9 +22,48 @@[m [mconst TEMPLATE_RESUMES = [[m
   }[m
 ];[m
 [m
[32m+[m[32mconst loadPdfJS = (): Promise<any> => {[m
[32m+[m[32m  return new Promise((resolve, reject) => {[m
[32m+[m[32m    if ((window as any).pdfjsLib) {[m
[32m+[m[32m      resolve((window as any).pdfjsLib);[m
[32m+[m[32m      return;[m
[32m+[m[32m    }[m
[32m+[m[32m    const script = document.createElement("script");[m
[32m+[m[32m    script.src = "https://cdnjs.cloudflare.com/ajax/libs/pdf.js/3.4.120/pdf.min.js";[m
[32m+[m[32m    script.onload = () => {[m
[32m+[m[32m      const pdfjsLib = (window as any).pdfjsLib;[m
[32m+[m[32m      pdfjsLib.GlobalWorkerOptions.workerSrc = "https://cdnjs.cloudflare.com/ajax/libs/pdf.js/3.4.120/pdf.worker.min.js";[m
[32m+[m[32m      resolve(pdfjsLib);[m
[32m+[m[32m    };[m
[32m+[m[32m    script.onerror = () => {[m
[32m+[m[32m      reject(new Error("Failed to load PDF parsing library. Check your network."));[m
[32m+[m[32m    };[m
[32m+[m[32m    document.head.appendChild(script);[m
[32m+[m[32m  });[m
[32m+[m[32m};[m
[32m+[m
[32m+[m[32mconst extractTextFromPdf = async (file: File): Promise<string> => {[m
[32m+[m[32m  const pdfjsLib = await loadPdfJS();[m
[32m+[m[32m  const arrayBuffer = await file.arrayBuffer();[m
[32m+[m[32m  const loadingTask = pdfjsLib.getDocument({ data: arrayBuffer });[m
[32m+[m[32m  const pdf = await loadingTask.promise;[m
[32m+[m[32m  let fullText = "";[m
[32m+[m[41m  [m
[32m+[m[32m  for (let i = 1; i <= pdf.numPages; i++) {[m
[32m+[m[32m    const page = await pdf.getPage(i);[m
[32m+[m[32m    const textContent = await page.getTextContent();[m
[32m+[m[32m    const pageText = textContent.items[m
[32m+[m[32m      .map((item: any) => item.str)[m
[32m+[m[32m      .join(" ");[m
[32m+[m[32m    fullText += pageText + "\n";[m
[32m+[m[32m  }[m
[32m+[m[32m  return fullText;[m
[32m+[m[32m};[m
[32m+[m
 export default function CandidateIngestionForm({ onIngestSuccess }: CandidateIngestionFormProps) {[m
   const [resumeText, setResumeText] = useState("");[m
   const [loading, setLoading] = useState(false);[m
[32m+[m[32m  const [parsingFile, setParsingFile] = useState(false);[m
   const [stageIndex, setStageIndex] = useState(0);[m
   const [error, setError] = useState<string | null>(null);[m
 [m
[36m@@ -65,31 +104,54 @@[m [mexport default function CandidateIngestionForm({ onIngestSuccess }: CandidateIng[m
     }[m
   };[m
 [m
[31m-  const processFile = (file: File) => {[m
[32m+[m[32m  const processFile = async (file: File) => {[m
     if (!file) return;[m
 [m
     const fileExtension = file.name.split(".").pop()?.toLowerCase();[m
[31m-    const isSupported = fileExtension === "txt" || fileExtension === "md" || fileExtension === "json";[m
[32m+[m[32m    const isSupported = fileExtension === "txt" || fileExtension === "md" || fileExtension === "json" || fileExtension === "pdf";[m
 [m
     if (!isSupported) {[m
[31m-      setError("Please upload a plain text (.txt), Markdown (.md), or JSON (.json) resume file.");[m
[32m+[m[32m      setError("Please upload a plain text (.txt), Markdown (.md), JSON (.json), or PDF (.pdf) resume file.");[m
       return;[m
     }[m
 [m
[31m-    const reader = new FileReader();[m
[31m-    reader.onload = (e) => {[m
[31m-      const text = e.target?.result as string;[m
[31m-      if (text) {[m
[32m+[m[32m    setParsingFile(true);[m
[32m+[m[32m    setError(null);[m
[32m+[m
[32m+[m[32m    try {[m
[32m+[m[32m      if (fileExtension === "pdf") {[m
[32m+[m[32m        const text = await extractTextFromPdf(file);[m
[32m+[m[32m        if (!text.trim()) {[m
[32m+[m[32m          throw new Error("No readable text found in PDF. Make sure it is not scanned or empty.");[m
[32m+[m[32m        }[m
[32m+[m[32m        setResumeText(text);[m
[32m+[m[32m        const sizeKB = (file.size / 1024).toFixed(1);[m
[32m+[m[32m        setUploadedFile({ name: file.name, size: `${sizeKB} KB` });[m
[32m+[m[32m      } else {[m
[32m+[m[32m        const text = await new Promise<string>((resolve, reject) => {[m
[32m+[m[32m          const reader = new FileReader();[m
[32m+[m[32m          reader.onload = (e) => {[m
[32m+[m[32m            const result = e.target?.result as string;[m
[32m+[m[32m            if (result) {[m
[32m+[m[32m              resolve(result);[m
[32m+[m[32m            } else {[m
[32m+[m[32m              reject(new Error("Empty file content."));[m
[32m+[m[32m            }[m
[32m+[m[32m          };[m
[32m+[m[32m          reader.onerror = () => reject(new Error("Error reading file."));[m
[32m+[m[32m          reader.readAsText(file);[m
[32m+[m[32m        });[m
         setResumeText(text);[m
         const sizeKB = (file.size / 1024).toFixed(1);[m
         setUploadedFile({ name: file.name, size: `${sizeKB} KB` });[m
[31m-        setError(null);[m
       }[m
[31m-    };[m
[31m-    reader.onerror = () => {[m
[31m-      setError("Error reading file.");[m
[31m-    };[m
[31m-    reader.readAsText(file);[m
[32m+[m[32m    } catch (err: any) {[m
[32m+[m[32m      console.error(err);[m
[32m+[m[32m      setError(err.message || "Error reading or parsing file.");[m
[32m+[m[32m      setUploadedFile(null);[m
[32m+[m[32m    } finally {[m
[32m+[m[32m      setParsingFile(false);[m
[32m+[m[32m    }[m
   };[m
 [m
   const handleDrop = (e: React.DragEvent) => {[m
[36m@@ -199,6 +261,8 @@[m [mexport default function CandidateIngestionForm({ onIngestSuccess }: CandidateIng[m
           className={`border-2 border-dashed rounded-xl p-5 text-center cursor-pointer transition-all duration-200 ${[m
             dragActive[m
               ? "border-[#024950] bg-[#024950]/5 scale-[0.99]"[m
[32m+[m[32m              : parsingFile[m
[32m+[m[32m              ? "border-[#3D52A0] bg-[#3D52A0]/5"[m
               : uploadedFile[m
               ? "border-emerald-200 bg-emerald-50/10"[m
               : "border-sand-200 hover:border-sand-300 bg-sand-50/30 hover:bg-sand-50/50"[m
[36m@@ -208,11 +272,23 @@[m [mexport default function CandidateIngestionForm({ onIngestSuccess }: CandidateIng[m
             ref={fileInputRef}[m
             type="file"[m
             onChange={handleFileChange}[m
[31m-            accept=".txt,.md,.json"[m
[32m+[m[32m            accept=".txt,.md,.json,.pdf"[m
             className="hidden"[m
           />[m
 [m
[31m-          {uploadedFile ? ([m
[32m+[m[32m          {parsingFile ? ([m
[32m+[m[32m            <div className="flex flex-col items-center justify-center py-2" onClick={(e) => e.stopPropagation()}>[m
[32m+[m[32m              <div className="p-2.5 bg-[#3D52A0]/10 rounded-xl text-[#3D52A0] mb-2 animate-spin">[m
[32m+[m[32m                <RefreshCw className="w-4 h-4" />[m
[32m+[m[32m              </div>[m
[32m+[m[32m              <p className="text-xs font-semibold text-[#3D52A0] animate-pulse">[m
[32m+[m[32m                Extracting text content from file...[m
[32m+[m[32m              </p>[m
[32m+[m[32m              <p className="text-[10px] text-sand-400 mt-1">[m
[32m+[m[32m                Using client-side high-fidelity extractor[m
[32m+[m[32m              </p>[m
[32m+[m[32m            </div>[m
[32m+[m[32m          ) : uploadedFile ? ([m
             <div className="flex items-center justify-between bg-white border border-emerald-100 p-2.5 rounded-lg shadow-2xs max-w-md mx-auto" onClick={(e) => e.stopPropagation()}>[m
               <div className="flex items-center gap-2 text-left">[m
                 <div className="p-2 bg-emerald-50 rounded-lg text-emerald-600">[m
[36m@@ -255,7 +331,7 @@[m [mexport default function CandidateIngestionForm({ onIngestSuccess }: CandidateIng[m
                 Drag and drop your resume file here, or <span className="text-[#024950] hover:underline">browse</span>[m
               </p>[m
               <p className="text-[10px] text-sand-400 mt-1">[m
[31m-                Supports .txt, .md, .json (Plain Text formats)[m
[32m+[m[32m                Supports .txt, .md, .json, and .pdf (Automatic Text Extraction)[m
               </p>[m
             </div>[m
           )}[m
[1mdiff --git a/src/components/ThemeToggle.tsx b/src/components/ThemeToggle.tsx[m
[1mnew file mode 100644[m
[1mindex 0000000..a95a465[m
[1m--- /dev/null[m
[1m+++ b/src/components/ThemeToggle.tsx[m
[36m@@ -0,0 +1,29 @@[m
[32m+[m[32mimport React, { useEffect } from "react";[m
[32m+[m[32mimport { Sun, Moon } from "lucide-react";[m
[32m+[m
[32m+[m[32mtype ThemeToggleProps = {[m
[32m+[m[32m  isDark: boolean;[m
[32m+[m[32m  toggleTheme: () => void;[m
[32m+[m[32m};[m
[32m+[m
[32m+[m[32mexport default function ThemeToggle({ isDark, toggleTheme }: ThemeToggleProps) {[m
[32m+[m[32m  // Ensure system sync on mount[m
[32m+[m[32m  useEffect(() => {[m
[32m+[m[32m    const classList = document.documentElement.classList;[m
[32m+[m[32m    if (isDark) {[m
[32m+[m[32m      classList.add("dark");[m
[32m+[m[32m    } else {[m
[32m+[m[32m      classList.remove("dark");[m
[32m+[m[32m    }[m
[32m+[m[32m  }, [isDark]);[m
[32m+[m
[32m+[m[32m  return ([m
[32m+[m[32m    <button[m
[32m+[m[32m      onClick={toggleTheme}[m
[32m+[m[32m      className="flex items-center gap-1.5 px-3 py-1.5 rounded-full text-sm font-medium bg-white/80 hover:bg-white border border-gray-200 shadow-sm"[m
[32m+[m[32m    >[m
[32m+[m[32m      {isDark ? <Sun className="w-4 h-4" /> : <Moon className="w-4 h-4" />}[m
[32m+[m[32m      {isDark ? "Light Mode" : "Dark Mode"}[m
[32m+[m[32m    </button>[m
[32m+[m[32m  );[m
[32m+[m[32m}[m
[1mdiff --git a/src/index.css b/src/index.css[m
[1mindex 0a4e435..45cbdd4 100644[m
[1m--- a/src/index.css[m
[1m+++ b/src/index.css[m
[36m@@ -126,3 +126,4 @@[m [minput, button, select, textarea {[m
   animation: float 4s ease-in-out infinite;[m
 }[m
 [m
[32m+[m[32m.dark body { background-color: var(--color-sand-900); color: var(--color-sand-100); }[m
